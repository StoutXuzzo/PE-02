
from math import pow, ceil, floor, pi # I can import only the things (functions and constants) 
#i need to use

print(pow(2,3)) # When you import using this tecnique, you don't need to put 
# the name of the imported module

import math

# On an imported class, the functios are marct as cubes and the constants as a blue "corchetes".

print(math.pow(2,3)) # This is a function, it has "parentesis".

print(math.pi) # This is a constant, it doesn't have "parentesis".

